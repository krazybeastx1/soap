version: "3.9"

services:
  web:
    build: .
    container_name: soap-company-portal
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - SOAP_SECRET=change-me-in-prod
    restart: unless-stopped
